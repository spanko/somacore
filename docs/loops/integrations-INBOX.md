# Integrations loop — inbox for Adam

The loop writes here when it hits something only you can decide or provide. Newest first. Delete entries as you handle them.

---

- **2026-07-12 (S3, one-line WHOOP change for your review):** `WhoopWebhookDrainer`'s claim query gained `AND source = 'whoop'` (src/SomaCore.Api/Whoop/WhoopWebhookDrainer.cs). Without it, the WHOOP drainer claims Strava rows from the shared webhook_events queue and permanently discards them as unsupported. Behavior-preserving for WHOOP (all pre-existing rows are source='whoop'); evaluator verified all WHOOP tests green and a regression test covers the filter (`Whoop_drainer_claim_does_not_touch_strava_rows`). Flagging because the brief's not-in-scope list says "any change to WHOOP ingestion" — this was the minimal seam required by the shared-queue design.
- **2026-07-12 (S1 evaluator finding, not blocking):** `WhoopStateProtectorTests.Should_reject_a_tampered_state_token` (tests/SomaCore.UnitTests/Whoop/WhoopStateProtectorTests.cs:38-41) is flaky — it flips one base64 char at an arithmetic midpoint, which occasionally fails to corrupt the AEAD tag, so the "tampered token rejected" assertion intermittently sees a valid token. Preexisting on `main`, unrelated to loop items; will occasionally fail CI. Suggested fix: flip a byte in the decoded ciphertext (or assert over several positions) instead of one base64 char. Also noted: `MigrationTests.cs` asserts a fixed legacy table list and doesn't cover new tables — schema drift in new migrations isn't behaviorally tested.
- **2026-07-10 (pre-seeded, M1):** when M1 completes against the synthetic fixture, the acceptance gate is YOUR real MFP data-export ZIP parsed against what the MFP UI shows. Request the export at myfitnesspal.com (desktop, Premium) — it arrives by email, takes a day sometimes. Worth kicking off before the loop reaches M1.
- **2026-07-10 (pre-seeded, S-items):** building S1–S7 needs nothing from you; DEPLOYING them needs the Strava dev account ($11.99/mo), the two KV secrets, and the webhook subscription registration (deployed endpoint first — chicken-and-egg order is in the session brief §1.1).
